# Build thorvg-cython on macOS + Windows + Linux, run tests
# https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  macos:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: "3.13"
    - uses: astral-sh/setup-uv@v5

    - name: Clone and build ThorVG
      run: |
        uv tool install meson
        git clone https://github.com/psychowasp/thorvg.git
        bash tools/thorvg_scripts/build_macos.sh thorvg

    - name: Run tests
      run: |
        THORVG_ROOT=thorvg THORVG_LIB_DIR=thorvg/output/macos_fat uv run pytest tests/ -v

  linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: "3.13"
    - uses: astral-sh/setup-uv@v5

    - name: Clone and build ThorVG
      run: |
        uv tool install meson
        git clone https://github.com/psychowasp/thorvg.git
        bash tools/thorvg_scripts/build_linux.sh thorvg

    - name: Run tests
      run: |
        THORVG_ROOT=thorvg THORVG_LIB_DIR=thorvg/output/linux_x86_64 uv run pytest tests/ -v

  windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: "3.13"
    - uses: astral-sh/setup-uv@v5

    - name: Clone and build ThorVG
      run: |
        uv tool install meson
        git clone https://github.com/psychowasp/thorvg.git
        call tools\thorvg_scripts\build_windows.bat thorvg

    - name: Run tests
      run: |
        $env:THORVG_ROOT="thorvg"
        $env:THORVG_LIB_DIR="thorvg\output\windows_x64"
        uv run pytest tests/ -v
