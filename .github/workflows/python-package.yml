# Build thorvg-cython on macOS, run tests
# https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.13"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install build tools
      run: |
        python -m pip install --upgrade pip
        python -m pip install meson ninja cython pytest

    - name: Clone and build ThorVG
      run: |
        git clone https://github.com/psychowasp/thorvg.git
        cd thorvg && bash build_all.sh

    - name: Build and install thorvg-cython
      run: |
        THORVG_ROOT=thorvg THORVG_LIB_DIR=thorvg/output/macos_fat pip install -e .

    - name: Run tests
      run: |
        pytest tests/ -v
